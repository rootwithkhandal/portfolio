# GitBook API token — get from gitbook.com → Settings → API Tokens
# Required for /blogs/[slug] pages to fetch content at build time
GITBOOK_TOKEN=gb_live_xxxxxxxxxxxxxxxxxxxx

# GitHub token — only needed for private repos
# Public repos work without a token
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
